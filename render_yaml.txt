services:
  - type: web
    name: telegram-gif-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 render_bot.py
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
