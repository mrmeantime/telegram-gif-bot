services:
  - type: web
    name: telegram-gif-bot
    env: python
    pythonVersion: 3.12.6   # 👈 Forces Render to use this
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r Requirements.txt
    startCommand: python3 render_bot.py
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
